# memory.md Template

This template represents the human-readable memory file for an agent workspace.

It should contain:

- synchronized summaries of durable agent memory
- important workspace facts and decisions
- channel- or user-specific reminders when surfaced for inspection

This file should be updated alongside structured `AgentMemory` records, not treated as the primary source of truth.
