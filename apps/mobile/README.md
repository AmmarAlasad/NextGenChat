# @nextgenchat/mobile

Phase 8 placeholder.

The mobile app is intentionally not scaffolded yet. When implemented it will:

- live in this directory as a React Native + Expo app
- reuse `@nextgenchat/types`
- connect to the same backend API and Socket.io contracts as web
- store tokens in Expo SecureStore, not AsyncStorage
