# Web Tests

The web package does not define a test runner yet.

Planned coverage from `plan.md`:

- Vitest + Testing Library for component and utility tests
- Playwright for end-to-end flows
- Setup wizard, auth, workspace navigation, and chat message streaming
