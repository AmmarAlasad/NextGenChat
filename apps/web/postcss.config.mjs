// PostCSS configuration for the web app.
// Currently loads Tailwind CSS v4 support and can be extended with future transforms.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
